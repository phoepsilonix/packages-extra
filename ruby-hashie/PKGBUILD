# Maintainer: artoo <artoo@manjaro.org>

_gemname_=hashie
pkgname=ruby-${_gemname_}
pkgver=3.4.4
pkgrel=1
pkgdesc='Hashie is a collection of classes and mixins that make hashes more powerful.'
arch=('any')
url='https://github.com/intridea/hashie'
license=('Custom')
depends=('ruby')
options=('!emptydirs')
noextract=("${_gemname_}-${pkgver}.gem")
source=("https://rubygems.org/downloads/${_gemname_}-${pkgver}.gem")
sha256sums=('9dd69627730107041421f453eebef585251a50f6ec2720de0bce2443736598e2')

package() {
		local _gemdir="$(ruby -e'puts Gem.default_dir')"
		gem install --ignore-dependencies --no-user-install --no-ri --no-rdoc -i "${pkgdir}/${_gemdir}" -n "${pkgdir}/usr/bin" "${_gemname_}-${pkgver}.gem"
}
