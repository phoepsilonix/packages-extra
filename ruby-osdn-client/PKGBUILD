# Maintainer: artoo <artoo@manjaro.org>

_gemname_=osdn-client
pkgname=ruby-${_gemname_}
pkgver=0.0.20160304
pkgrel=1
pkgdesc='OSDN API basic client library for ruby'
arch=('any')
url='https://osdn.jp/'
license=('MIT')
depends=('ruby-typhoeus' 'ruby-json')
options=('!emptydirs')
noextract=("${_gemname_}-${pkgver}.gem")
source=("https://rubygems.org/downloads/${_gemname_}-${pkgver}.gem")
sha256sums=('1044f42e8174b60399694be170eb558ea67000a274c87ba97ecf8ef696c6bb65')

package() {
		local _gemdir="$(ruby -e'puts Gem.default_dir')"
		gem install --ignore-dependencies --no-user-install -i "${pkgdir}/${_gemdir}" -n "${pkgdir}/usr/bin" "${_gemname_}-${pkgver}.gem"
}
