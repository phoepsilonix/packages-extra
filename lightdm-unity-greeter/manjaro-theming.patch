diff -Nur a/data/com.canonical.unity-greeter.gschema.xml b/data/com.canonical.unity-greeter.gschema.xml
--- a/data/com.canonical.unity-greeter.gschema.xml	2012-04-17 09:14:44.000000000 +0200
+++ b/data/com.canonical.unity-greeter.gschema.xml	2012-05-19 14:53:40.022154293 +0200
@@ -2,11 +2,11 @@
 <schemalist gettext-domain="unity-greeter">
   <schema id="com.canonical.unity-greeter" path="/com/canonical/unity-greeter/">
     <key name="background" type="s">
-      <default>'/usr/share/backgrounds/warty-final-ubuntu.png'</default>
+      <default>'/usr/share/backgrounds/manjaro.jpg'</default>
       <summary>Background file to use, either an image path or a color (e.g. #772953)</summary>
     </key>
     <key name="background-color" type="s">
-      <default>'#2C001E'</default>
+      <default>'#76B046'</default>
       <summary>Background color (e.g. #772953), set before wallpaper is seen</summary>
     </key>
     <key name="draw-user-backgrounds" type="b">
@@ -26,15 +26,15 @@
       <summary>Background logo file to use</summary>
     </key>
     <key name="theme-name" type="s">
-      <default>'Ambiance'</default>
+      <default>'Adwaita-Manjaro-dark'</default>
       <summary>GTK+ theme to use</summary>
     </key>
     <key name="icon-theme-name" type="s">
-      <default>'ubuntu-mono-dark'</default>
+      <default>'Faenza-Green'</default>
       <summary>Icon theme to use</summary>
     </key>
     <key name="font-name" type="s">
-      <default>'Ubuntu 11'</default>
+      <default>'Sans 10'</default>
       <summary>Font to use</summary>
     </key>
     <key name="xft-antialias" type="b">
@@ -79,7 +79,7 @@
       <summary>Whether to enable the screen reader</summary>
     </key>
     <key name="play-ready-sound" type="b">
-      <default>true</default>
+      <default>false</default>
       <summary>Whether to play sound when greeter is ready</summary>
     </key>
   </schema>
diff -Nur a/src/session-list.c b/src/session-list.c
--- a/src/session-list.c	2012-04-17 09:08:57.000000000 +0200
+++ b/src/session-list.c	2012-05-19 16:30:18.517407815 +0200
@@ -749,6 +749,7 @@ static gchar* session_list_get_badge_nam
 	static GQuark _tmp2_label7 = 0;
 	static GQuark _tmp2_label8 = 0;
 	static GQuark _tmp2_label9 = 0;
+	static GQuark _tmp2_label10 = 0;
 	g_return_val_if_fail (session != NULL, NULL);
 	_tmp0_ = session;
 	_tmp1_ = _tmp0_;
@@ -802,6 +803,16 @@ static gchar* session_list_get_badge_nam
 				result = _tmp8_;
 				return result;
 			}
+		}
+	} else if (_tmp3_ == ((0 != _tmp2_label10) ? _tmp2_label10 : (_tmp2_label10 = g_quark_from_static_string ("xfce")))) {
+		switch (0) {
+			default:
+			{
+				gchar* _tmp8_;
+				_tmp8_ = g_strdup ("xfce_badge.png");
+				result = _tmp8_;
+				return result;
+			}
 		}
 	} else {
 		switch (0) {
diff -Nur a/src/session-list.vala b/src/session-list.vala
--- a/src/session-list.vala	2012-03-18 23:38:33.000000000 +0100
+++ b/src/session-list.vala	2012-05-19 16:32:02.613510544 +0200
@@ -41,6 +41,7 @@ public class SessionPrompt : PromptBox
             box.add_item ("gnome", "GNOME", SessionList.get_badge ("gnome"));
             box.add_item ("kde", "KDE", SessionList.get_badge ("kde"));
             box.add_item ("ubuntu", "Ubuntu", SessionList.get_badge ("ubuntu"));
+            box.add_item ("xfce", "XFCE", SessionList.get_badge ("xfce"));
         }
         else
         {
@@ -109,6 +110,8 @@ public class SessionList : GreeterList
             return "kde_badge.png";
         case "xterm":
             return "recovery_console_badge.png";
+        case "xfce":
+            return "xfce_badge.png";
         case "remote-login":
             return "remote_login_help.png";
         default:
